# Scripts

This folder is for runnable operational helpers that are still useful outside the core application packages.

Prefer `scripts/` for repeatable local/admin commands and `tools/` for very small ad hoc helpers. If a script becomes part of a repeatable data pipeline, move it into `etl/` or `analytics/` instead.
