# Config

This folder is the canonical home for runtime and analysis configuration files.

Current usage:
- `ingestion_config.json` - source registry and confidence/bias metadata for ingestion jobs
- `pyrightconfig.json` - type-checking settings used by the editor and CI

Note: the repository still keeps root-level compatibility files where tooling expects them.
